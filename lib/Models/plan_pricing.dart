class PlanPricing {
  bool? movies, tv, ad;
  int? screenNumber;
  String? resolution, price, name;

  PlanPricing(this.movies, this.tv, this.ad, this.screenNumber, this.resolution,
      this.price, this.name);
}
